# Siempre usaras el enum de estados  ubicado en enums/state.enum.ts para establecer estados en servicios y componentes de la app.

# Todos los formatos de enums deben ser en blokes tipo columnas asi como mi enum de state.enum.ts.

# Siempre debes usar la nueva sintaxis control flow angular moderna.

# siempre debe usar Angular Material Puro para UI al crear vistas de componentes.